import { BlockHealthColorPipe } from './block-health-color.pipe';

describe('BlockHealthColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockHealthColorPipe();
    expect(pipe).toBeTruthy();
  });
});
