import { SparklineDataPipe } from './sparkline-data.pipe';

describe('SparklineDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SparklineDataPipe();
    expect(pipe).toBeTruthy();
  });
});
