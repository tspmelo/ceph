import { PoolSizeBarPipe } from './pool-size-bar.pipe';

describe('PoolSizeBarPipe', () => {
  it('create an instance', () => {
    const pipe = new PoolSizeBarPipe();
    expect(pipe).toBeTruthy();
  });
});
