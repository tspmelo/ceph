import { OsdSparklineDataPipe } from './osd-sparkline-data.pipe';

describe('OsdSparklineDataPipe', () => {
  it('create an instance', () => {
    const pipe = new OsdSparklineDataPipe();
    expect(pipe).toBeTruthy();
  });
});
