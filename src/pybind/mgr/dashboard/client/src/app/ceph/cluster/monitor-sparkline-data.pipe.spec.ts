import { MonitorSparklineDataPipe } from './monitor-sparkline-data.pipe';

describe('MonitorSparklineDataPipe', () => {
  it('create an instance', () => {
    const pipe = new MonitorSparklineDataPipe();
    expect(pipe).toBeTruthy();
  });
});
