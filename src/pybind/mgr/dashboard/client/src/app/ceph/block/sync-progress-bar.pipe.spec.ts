import { SyncProgressBarPipe } from './sync-progress-bar.pipe';

describe('SyncProgressBarPipe', () => {
  it('create an instance', () => {
    const pipe = new SyncProgressBarPipe();
    expect(pipe).toBeTruthy();
  });
});
